# ── Java / Maven ──────────────────────
backend/target/
# 多模块后每个子模块都有自己的 target/(旧规则只盖住了单体时代的 backend/target)
backend/**/target/
*.class
*.jar
*.war
*.log
hs_err_pid*

# ── Node / Vite ───────────────────────
# 不锚路径: 在仓库根误跑一次 `npx vitest` 就会在根建出 node_modules/.vite/ 缓存,
# 锚定的规则看不见它, git status 里就多出一个不该有的未跟踪目录。
node_modules/
frontend/dist/
frontend/*.tsbuildinfo
*.tsbuildinfo
npm-debug.log*

# ── 本地环境变量 / 密钥 ───────────────
.env
.env.local
.env.*.local
*.pem
*.key

# ── 编辑器 / 系统 ─────────────────────
.idea/
.vscode/
.DS_Store
Thumbs.db

# ── 运行时数据 ────────────────────────
/data/
*.sqlite
__pycache__/
.gradle/
**/build/
